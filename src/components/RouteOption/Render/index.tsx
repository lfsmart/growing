import { ReactNode } from "react";
import { RouteChildrenProps } from "react-router-dom";

export const Render = (props: RouteChildrenProps): ReactNode => {
  return (
    <div>Children Render!</div>
  )
}